{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "video-editor.serviceAccountName" . }}
  labels:
    {{- include "video-editor.labels" . | nindent 4 }}
{{- end }}
