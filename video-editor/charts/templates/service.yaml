apiVersion: v1
kind: Service
metadata:
  name: {{ include "video-editor.fullname" . }}
  labels:
    {{- include "video-editor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "video-editor.selectorLabels" . | nindent 4 }}