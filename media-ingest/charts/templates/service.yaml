apiVersion: v1
kind: Service
metadata:
  name: {{ include "mediaingest.fullname" . }}
  labels:
    {{- include "mediaingest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mediaingest.selectorLabels" . | nindent 4 }}
