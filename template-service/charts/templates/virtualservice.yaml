apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ .Values.virtualService.name }}
spec:
  hosts:
  {{- range .Values.virtualService.hosts }}
  - {{ . | quote }}
  {{- end }}
  # internal service communication
  - "{{ include "template-service.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local"
  gateways:
  - {{ .Values.gateway.name }}
  http:
  - match:
    - uri:
        prefix: "/template"
    rewrite:
      uri: "/"
    corsPolicy:
      allowOrigins:
      {{- range .Values.cors.allowedOrigins }}
      - exact: {{ . | quote }}
      {{- end }}
      {{- range .Values.cors.allowedOriginsRegex }}
      - regex: {{ . | quote }}
      {{- end }}
      allowMethods: ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
      allowHeaders: ["*"]
      allowCredentials: true
      maxAge: "24h"
    route:
    - destination:
        host: "{{ include "template-service.fullname" . }}"
        port:
          number: {{ .Values.service.port }}
    timeout: {{ .Values.virtualService.http.timeout }}
    retries:
      attempts: {{ .Values.virtualService.http.retries.attempts }}
      perTryTimeout: {{ .Values.virtualService.http.retries.perTryTimeout }}
      retryOn: {{ .Values.virtualService.http.retries.retryOn }}
